import { NotesfilterPipe } from './notesfilter.pipe';

describe('NotesfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NotesfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
